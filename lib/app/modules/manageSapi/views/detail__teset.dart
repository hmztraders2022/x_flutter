import 'package:flutter/material.dart';

class DetailTeset extends StatelessWidget {
  const DetailTeset({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TEST'),
      ),
      body: Center(
        child: Text('WORKING'),
      ),
    );
  }
}
